apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: salama-java-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: salama.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: salama-java-service
            port:
              number: 80
